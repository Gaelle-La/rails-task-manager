<h1>My tasks</h1>
<ul style="list-style-type: none">
  <% @tasks.each do |task| %>
    <li><%= check_box_tag :completed, '1', task.completed %><%= link_to task.title, task_path(task)%></li>
  <% end %>
</ul>
<%= link_to "+ Add a new task", new_task_path%>
